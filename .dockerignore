# Bỏ qua các tệp và thư mục không cần thiết khi build image
# để giữ cho image nhỏ gọn và an toàn.

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Thư mục chứa các dependency đã cài đặt (sẽ được cài lại bên trong container)
node_modules

# Tệp môi trường local (thông tin nhạy cảm không nên đưa vào image)
.env

# Các tệp cấu hình của Docker
Dockerfile
.dockerignore

# Các tệp hệ thống
.DS_Store
